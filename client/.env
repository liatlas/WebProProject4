# Backend API URL (where your Express server is running)
REACT_APP_API_URL=http://localhost:5000/api

# Socket.IO URL (same as your Express server, without /api)
REACT_APP_SOCKET_URL=http://localhost:5000

# PRODUCTION CONFIGURATION
# When deploying to production, change these to your actual domain:

# REACT_APP_API_URL=https://your-domain.com/api
# REACT_APP_SOCKET_URL=https://your-domain.com

# NOTES:
# - The REACT_APP_ prefix is required for Create React App to expose these
#   variables to the client-side code
# - Do not add quotes around the values
# - Restart the development server after changing these values
# - For production builds, set these before running 'npm run build'
